name: Codex Fixer

on:
  push:
    branches: [ main ]
  workflow_dispatch:        # allow manual run
  schedule:
    - cron: "0 2 * * *"     # run daily at 2 AM UTC

jobs:
  fix:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0   # needed so git tags are available

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "18"

      - name: Install deps
        run: npm install

      - name: Lint code with ESLint
        run: npx eslint . || true    # non-blocking

      - name: Format code with Prettier
        run: npx prettier --write . || true   # non-blocking

      - name: Create version tag and update VERSION.txt
        run: |
          count=$(git tag -l "codex-v*" | wc -l)
          new=$((count + 1))
          tag="codex-v$new"

          echo "$tag" > VERSION.txt
          git config user.name "github-actions"
          git config user.email "actions@github.com"

          git add VERSION.txt
          git commit -m "Update VERSION.txt to $tag" || echo "No changes"
          git tag "$tag"
          git push origin main --tags

